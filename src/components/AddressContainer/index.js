import React from "react";

import * as Styles from "./styles";

const AddressContainer = ({ address }) => {
  //   console.log(address);
  return <Styles.Wrapper>AddressContainer </Styles.Wrapper>;
};

export default AddressContainer;
