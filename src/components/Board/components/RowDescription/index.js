import React from "react";

import Row from "components/Board/components/Row";

const RowDescription = ({ first, second, third, fourth }) => {
  return <Row first={first} second={second} third={third} fourth={fourth} />;
};

export default RowDescription;
